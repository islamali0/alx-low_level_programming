#include <stdio.h>

/**
 * Main - prints all single digit numbers of base 10 starting from 0
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	int x;

	for (int x = 0; x <= 10; x++)
		printf("%d ,x");

	printf("\n");
	return (0);
}
